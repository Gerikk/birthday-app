<resources>
    <!-- Base application theme. -->
    <style name="Theme.Birthdayappfront" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAppearanceSmall">@style/Theme.AppCompat</item>
        <!--<item name="android:textAppearanceMedium">@style/customfontstyle</item>
        <item name="android:textAppearanceButton">@style/customfontstyle</item>-->
    </style>

    <style name="SplashScreenTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
    </style>

    <style name="AppTheme.Button" parent="Widget.AppCompat.Button.Borderless.Colored">

        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="Theme.Birthdayappfront.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Birthdayappfront.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Birthdayappfront.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--<style name="customfontstyle" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">@font/raleway</item>
    </style>-->
</resources>